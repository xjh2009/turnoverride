# Gradle
# IPv4 only (Cloudflare 的 IPv6 在国内常超时)
org.gradle.jvmargs=-Xmx2G -Djava.net.preferIPv4Stack=true
org.gradle.parallel=true

# 我们用 1.21.5 来构建——p2p 包名 / Mixin target 全靠 FQN 字符串,
# 与 mappings/MC 版本解耦,生成的 jar 仍可在 26.2-snapshot-7 上工作。
minecraft_version=1.21.5
loader_version=0.16.10

# Mod
mod_version=1.0.0
maven_group=com.xjhya.turnoverride
archives_base_name=turnoverride
